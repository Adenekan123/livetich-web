'use client';

// Phosphor uses React context internally, so it must cross a client boundary
// before the server-rendered landing page can use these icons.
export {
  Broadcast,
  PenNib,
  ChatCircleText,
  Lightning,
  Trophy,
  Certificate,
  Check,
  ArrowRight,
  SealCheck,
  NotePencil,
  CalendarCheck,
  UsersThree,
  Monitor,
  GraduationCap,
} from '@phosphor-icons/react';
